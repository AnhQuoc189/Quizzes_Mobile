import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const CatogoriesFilter = () => {
    return (
        <View>
            <Text>CatogoriesFilter</Text>
        </View>
    );
};

export default CatogoriesFilter;

const styles = StyleSheet.create({});
